@model AddRedirectUriViewModel


<div class="modal fade" id="addRedirectUri-modal" tabindex="-1" role="dialog" aria-labelledby="addRedirectUriModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="addRedirectUriModalLabel">افزودن Redirect Uri</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <partial name="_Message" />
                <form method="post" asp-action="AddRedirectUri">
                    <input type="hidden" asp-for="ClientId" />
                     <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                <label asp-for="RedirectUri">آدرس</label>
                                <input asp-for="RedirectUri" class="form-control ltr" />
                                <span asp-validation-for="RedirectUri" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </form>
                    
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">بستن</button>
                <button type="button" data-save="modal" class="btn btn-primary">ذخیره</button>
            </div>
        </div>
    </div>
</div>
